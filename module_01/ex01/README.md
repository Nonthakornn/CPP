# Moar brainz!

- Implement the following function
  - `Zombie *zombieHorde(int N, std::string name);`
- It must allocate N Zombie in a single allocation.
- It must initialize the zombies, giving each of them the name passed as parameter.
- The function return a pointer to the first zombie.